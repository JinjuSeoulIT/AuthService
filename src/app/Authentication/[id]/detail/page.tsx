// import AuthenticationDetail from "@/components/employee/Authentication/detail/AuthenticationDetail";

// type DetailPageProps = {
//   params: Promise<{ id: string }>;
// };

// export default async function DetailPage({ params }: DetailPageProps) {
//   const { id } = await params;
//   const authenticationId = Number(id);
//   return <AuthenticationDetail authenticationId={authenticationId} />;
// }