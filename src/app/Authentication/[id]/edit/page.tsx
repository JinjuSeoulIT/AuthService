// import AuthenticationUpdate from "@/components/employee/Authentication/update/AuthenticationUpdate";

// type PageProps = {
//   params: Promise<{ id: string }>;
// };

// export default async function EditPage({ params }: PageProps) {
//   const { id } = await params;
//   const authenticationId = Number(id);
//   return <AuthenticationUpdate authenticationId={authenticationId} />;
// }
